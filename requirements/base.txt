# Base runtime dependencies for the project
# Pin versions as needed. Example: transformers==4.35.0
transformers
datasets
trl
lm_eval
langdetect

# Add other runtime libraries you use in production (torch, tensorflow, etc.)
# Example:
# torch>=2.0
